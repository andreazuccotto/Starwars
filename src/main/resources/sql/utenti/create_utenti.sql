CREATE TABLE UTENTI (ID SERIAL PRIMARY KEY, USERNAME TEXT UNIQUE NOT NULL, PASSWORD TEXT NOT NULL, ATTIVO BOOLEAN DEFAULT FALSE, TIPO_UTENTE SMALLINT DEFAULT 1, DATA_REGISTRAZIONE TIMESTAMP DEFAULT NOW(), DATA_ULTIMO_ACCESSO TIMESTAMP DEFAULT NOW(), EMAIL TEXT NOT NULL);