CREATE TABLE UTENTI (USERNAME VARCHAR(30) PRIMARY KEY, PASSWORD VARCHAR(30) NOT NULL, ATTIVO BOOLEAN NOT NULL, TIPO_UTENTE INT NOT NULL, DATA_REGISTRAZIONE DATE, DATA_ULTIMO_ACCESSO DATE, EMAIL VARCHAR(50) NOT NULL);